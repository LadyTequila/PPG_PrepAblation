# 预处理代码组件化重构方案

## 一、原 v2 代码的组件归属定位

把 `prep_mesa_v2.py` 当前实现按 C1~C7 七个组件**重新归类**——这是后续拆分的依据。

| 组件 | 原 v2 中的实现位置 | 行号 | 当前选项 | 候选扩展 |
|------|------------------|------|---------|---------|
| **C1 时窗裁剪** | `get_sleep_window()` 函数 + `process_subject` 中的 `t_start_min/max` 计算 | L89~130, L340~341 | (b) XML 分期精确 | (a) 固定 30min / (c) 无裁剪 |
| **C2 Hypopnea 二次确认** | `parse_events_v2()` 函数 L172~185 "紧邻下一条 ScoredEvent" | L135~194 | (b) AASM 紧邻 | (a) 不确认 / (c) Flow ≥30% 严格 |
| **C3 信号归一化** | **当前 v2 不在 pickle 阶段做归一化**——由训练侧 `model_v2/runner.py` 的 StandardScaler 在每折拟合 | (无) | (a) 训练侧 StandardScaler | (b) DRIVEN 95% 分位 / (c) SleepPPG-Net2 3σ 裁剪 |
| **C4 滤波** | `ppg_extraction()` 中 `signal.cheby2(2, 0.1, 20, "hp", ...)` | L249 | (a) Cheby II 高通 20Hz | (b) Cheby II 低通 8Hz |
| **C5 标签粒度** | `make_label_array()` 中 `soft_label` 参数控制 | L205~232 | (b) 软标签 ratio | (a) 硬二值 / (c) 事件类型 mode |
| **C6 延迟对齐** | `make_label_array()` 中 `delay_sec` 参数 | L221 | (b) 固定 10s | (a) 无 / (c) 多延迟通道 |
| **C7 窗口/步长** | `process_subject` 的 `win_size` 参数 + `stride = win_size // 2` | L337 | (a) 60s/30s | (b) 30s/5s |

**一个重要观察**：

**v2 现状的 C3 是"训练侧归一化"**，即每个 fold 的 StandardScaler 是在 `model_v2/runner.py` 的训练管线里现拟合的，pickle 文件保存的是**未归一化**的原始特征张量。若要纳入 DRIVEN/SleepPPG-Net2 的归一化策略，需要决定**归一化时机**——见第三节"设计选择"。

---

## 二、新版主流程伪代码

### 2.1 主入口 `preprocess_modular.py`

```python
def preprocess_main(npz_dir, xml_dir, out_dir,
                    # 组件参数（决定 ablation 表的某一行）
                    trim_strategy="xml",         # C1: "fixed_30min" / "xml" / "none"
                    hypopnea_confirm="adjacent", # C2: "none" / "adjacent" / "flow_strict"
                    normalization="none",        # C3: "none" / "robust_95" / "3sigma_clip"
                    filter_mode="cheby_hp_20hz", # C4: "cheby_hp_20hz" / "cheby_lp_8hz"
                    label_mode="soft",           # C5: "hard" / "soft" / "mode"
                    delay_mode="single_10",      # C6: "none" / "single_10" / "multi"
                    win_size=60, stride=30,      # C7
                    # 其他
                    n_folds=5, random_seed=42, ahi_threshold=15,
                    experiment_id="row_02_v2_baseline"):
    """主入口。所有组件可通过参数切换。"""
    exp_dir = out_dir / experiment_id
    os.makedirs(exp_dir, exist_ok=True)

    # 1. 遍历受试者
    for npz_path, xml_path, subj_id in enumerate_subjects(npz_dir, xml_dir):
        result = process_subject_modular(
            npz_path, xml_path,
            trim_strategy=trim_strategy,
            hypopnea_confirm=hypopnea_confirm,
            normalization=normalization,
            filter_mode=filter_mode,
            label_mode=label_mode,
            delay_mode=delay_mode,
            win_size=win_size, stride=stride,
        )
        # ... 累积到 all_features / all_labels / all_t_starts
    
    # 2. 5 折划分 + pickle 落盘（结构与 v2 一致）
    save_folds(exp_dir, ...)
    
    # 3. 导出 subjects_index.json（含本次实验的组件配置）
    save_subjects_index(exp_dir, ..., config={
        "trim_strategy": trim_strategy, ...
    })
```

### 2.2 单受试者处理 `process_subject_modular`

```python
def process_subject_modular(npz_path, xml_path, **components):
    # 读信号
    ppg, spo2, flow, fs_ppg, fs_spo2, fs_flow = load_signals(npz_path)
    
    # C1: 时窗裁剪
    sleep_window = trim.get_window(xml_path, strategy=components["trim_strategy"])
    if sleep_window is None: return None
    onset, offset = sleep_window
    
    # C2: 事件确认
    events = confirm_hypopnea.parse(
        xml_path, mode=components["hypopnea_confirm"],
        flow=flow, fs_flow=fs_flow,  # flow_strict 模式才用
    )
    
    # 计算 AHI
    ahi = (len(events) / ((offset - onset) / 3600))
    
    # 滑窗循环
    win_size = components["win_size"]
    stride = components["stride"]
    
    features_list, labels_list, t_starts_list = [], [], []
    for t_start in range(int(onset), int(offset) - win_size, stride):
        # SpO2 质量过滤
        if not spo2_quality_ok(spo2, fs_spo2, t_start, win_size): continue
        
        # 提取 PPG 窗口
        ppg_win = ppg[int(t_start*fs_ppg) : int((t_start+win_size)*fs_ppg)]
        if len(ppg_win) < win_size * fs_ppg: continue
        
        # C4: 滤波（在 ppg_extraction 内部调用）
        # C3: 归一化（窗口级，紧接在 extraction 之后）
        try:
            feat = filter.ppg_extraction(
                ppg_win, hz=int(fs_ppg), win_size=win_size,
                filter_mode=components["filter_mode"],
            )
            feat = normalize.apply(feat, mode=components["normalization"])
        except Exception:
            continue
        
        # C5+C6: 标签生成
        label = label.make_array(
            events, t_start, win_size,
            label_mode=components["label_mode"],
            delay_mode=components["delay_mode"],
        )
        
        features_list.append(feat)
        labels_list.append(label)
        t_starts_list.append(int(t_start))
    
    return {
        "features": features_list,
        "labels": labels_list,
        "t_starts": t_starts_list,
        "ahi": ahi,
        "sleep_window": (onset, offset),
        "events": events,
    }
```

---

## 三、需要你拍板的 4 个设计选择

### 选择 1 ：C3 归一化的时机

**问题**：DRIVEN 和 SleepPPG-Net2 都在"信号→pickle"阶段做归一化（patient-level / window-level）；本组 v2 是在"pickle→训练"阶段（fold-level StandardScaler）。两种时机的语义不同。

**选项**：
- **A. 统一改到 pickle 阶段**（推荐）：所有 ablation 行都在 pickle 阶段做归一化，训练侧关掉 StandardScaler。**优点**：所有行公平可比。**代价**：v2 等价配置（`normalization="none"`）下，pickle 内容跟原 `MESA_v2/delay10_win60_soft1/` 保持一致，但若想跑训练，必须修改 `model_v2/runner.py` 跳过 StandardScaler（或验证 fold-level StandardScaler 在已归一化数据上是恒等的）。
- **B. 时机混合**：v2 配置仍走训练侧归一化；DRIVEN/SleepPPG 配置走 pickle 阶段归一化。**优点**：零侵入毕设管线。**代价**：归一化"时机"也成了被对比的变量之一，无法纯粹对比"归一化算法本身的优劣"。
- **C. 在 pickle 阶段加一个开关**：默认不做归一化（与原 v2 一致），但提供 `--normalization=robust_95` 等开关。训练侧 StandardScaler 加一个开关 `--skip-scaler`，由用户保证两边一致。**优点**：灵活、不破坏现状。**代价**：用户必须正确组合两端开关。

**我的建议**：选 **C**——既向后兼容毕设管线（默认行为 = v2），又支持新的归一化策略；用户操作时只要 ablation 实验配置文件里写明"pickle 端归一化 X + 训练端 skip-scaler"即可。

---

### 选择 2 ：C6 多延迟通道的张量形状

**问题**：DRIVEN 的"多延迟"实际上是把 SpO₂ 同时按 0/10/15/20/25 秒生成 5 个独立通道作为模型输入。本组若纳入这一思想，要决定**标签变多通道还是信号变多通道**？

**选项**：
- **A. 标签变多通道**：每窗口的标签 array 从 `(win_size,)` 扩展到 `(n_delays, win_size)`。模型仍然只看一个标签，但训练时可以选择"使用哪个延迟的标签"，相当于做 5 次单延迟训练。**优点**：实现简单。**代价**：不是 DRIVEN 真正的"多延迟并行通道"。
- **B. 信号变多通道**：把原始 PPG 时域信号按多个延迟切窗，得到 `(n_delays, 7, win_size)` 的张量。**优点**：忠实 DRIVEN 思路。**代价**：模型输入头要改（从 (7, 60) 变 (5*7, 60) 或 (5, 7, 60)），DSepST15Net 基线不能直接接。
- **C. 信号通道维拼接**：把多延迟的信号沿通道维拼接，得到 `(n_delays * 7, win_size)` = `(35, win_size)`。**优点**：模型输入只是通道数从 7 变 35。**代价**：DSepST15Net 第一层卷积要改 in_channels=35（小改动）。

**我的建议**：选 **A**——本工作的核心是"对比预处理组件对单延迟训练的影响"，"多延迟"组件按"五个延迟下分别独立训练 5 次，取最优"的方式做最公平。后续若想真正做"多延迟并行通道"作为多模态扩展，可以放到第六章路径 A 里。

---

### 选择 3 ：C2 (c) Flow 严格判定的具体阈值

**问题**：AASM 原始定义是"气流下降 ≥30% 持续 ≥10 秒"。要把这条用 Python 实现，需要决定几个细节。

**选项**：
- **基线幅值怎么算**：(a) 前 30 秒平均；(b) 整夜中位数；(c) 滑动 60 秒中位数
- **"≥30% 下降"判定窗口**：与候选事件时间区间完全重合（精确）/ 候选事件 ± 5 秒（容忍标注偏差）
- **持续时长**：必须 ≥10 秒连续，还是允许间断 ≤2 秒

**我的建议**：先按"滑动 60 秒中位数作为基线 + 候选事件 ± 5 秒窗口内 30% 下降 + ≥10 秒连续"的相对宽松版本实现。如果消融结果显示这条组件的边际贡献很小，再考虑做敏感性分析。**这部分需要写一个独立的 `flow_severity.py` 工具模块，是本研究的"原创代码量"主要部分**。

---

### 选择 4 ：实验输出目录的命名约定

**问题**：每个 ablation 行的输出 pickle 放在 `ra_studies/MESA_ablation/` 下的哪个子目录？

**选项**：
- **A. 按消融表行号**：`row_02_v2_baseline/` / `row_03_driven_norm/` / `row_04_sleepppg_norm/` …
- **B. 按组件配置字符串**：`xml_adj_none_chebyHP_soft_d10_60s/` （组合 7 个组件值）
- **C. 短哈希**：`exp_a3f7b2/`，配合 `experiments.csv` 总表查找

**我的建议**：选 **A**——人类可读、与消融表行号一对一对应、便于汇报。同时在每个目录里多保存一个 `config.json` 记录该行的具体组件值（避免歧义）。

---

## 四、新版文件结构（详细版）

```
ApSense-main/ra_studies/
├── 研究计划_预处理对比消融.md         (已有)
├── 重构方案.md                       (本文档)
├── preprocess_modular.py             ← 主入口
├── preprocessing_components/
│   ├── __init__.py
│   ├── trim.py                       ← C1
│   ├── confirm_hypopnea.py           ← C2（含 Flow 严格判定）
│   ├── flow_severity.py              ← C2 子模块：Flow 信号下降分析
│   ├── normalize.py                  ← C3（StandardScaler 占位 / DRIVEN 95% / 3σ 裁剪）
│   ├── filter.py                     ← C4（含原 ppg_extraction）
│   ├── label.py                      ← C5+C6（含原 make_label_array）
│   └── windowing.py                  ← C7（滑窗循环本身）
├── test_reproduce_v2.py              ← 验证脚本
├── MESA_ablation/                    ← 各 ablation 行的产物（自动创建）
│   ├── row_02_v2_baseline/
│   │   ├── mesa_fold0_x_train.pickle  (与 MESA_v2/delay10_win60_soft1/ 一致)
│   │   ├── ...
│   │   ├── config.json               ← 本行的组件配置
│   │   └── fold_info.txt
│   ├── row_03_driven_norm/
│   ├── row_04_sleepppg_norm/
│   ├── ...
│   └── EXPERIMENTS.md                ← 全部行的索引（一目了然）
├── run_ablation.py                   (Week 3 才写)
└── results/                          (Week 4 才用)
```

---

## 五、组件接口签名草案

### `trim.py`

```python
def get_window(xml_path: str, strategy: str = "xml") -> tuple[float, float] | None:
    """
    返回 (sleep_onset_sec, sleep_offset_sec) 或 None。

    strategy:
      "fixed_30min" → (1800, recording_duration - 1800)
      "xml"         → 复用原 prep_mesa_v2.get_sleep_window 逻辑
      "none"        → (0, recording_duration)
    """
```

### `confirm_hypopnea.py`

```python
def parse(xml_path: str,
          mode: str = "adjacent",
          flow: np.ndarray | None = None,
          fs_flow: float | None = None,
          ) -> tuple[list[tuple[float, float]], dict]:
    """
    返回 (events, stats)。

    mode:
      "none"        → 所有 Apnea + Hypopnea + Unsure 直接全部保留
      "adjacent"    → 复用原 prep_mesa_v2.parse_events_v2 逻辑（紧邻确认）
      "flow_strict" → Apnea 直留 + Hypop/Unsure 按 Flow ≥30% 下降 ≥10s 严格判定
    """
```

### `normalize.py`

```python
def apply(feat: np.ndarray, mode: str = "none") -> np.ndarray:
    """
    feat: (7, RESAMPLE_N) PPG 形态特征。
    mode:
      "none"        → 原样返回（v2 等价）
      "robust_95"   → 每通道分别做 95% 分位 z-score（DRIVEN 风格）
      "3sigma_clip" → 每通道分别做 3σ 裁剪 + z-score（SleepPPG-Net2 风格）
    """
```

### `filter.py`

```python
def ppg_extraction(raw_ppg: np.ndarray, hz: int, win_size: int,
                   filter_mode: str = "cheby_hp_20hz") -> np.ndarray:
    """
    返回 (7, RESAMPLE_N) PPG 形态特征。
    filter_mode:
      "cheby_hp_20hz" → 原 prep_mesa_v2 的 Cheby II 2 阶高通 20Hz
      "cheby_lp_8hz"  → SleepPPG-Net2 风格的 Cheby II 8 阶低通 8Hz
    """
```

### `label.py`

```python
def make_array(events: list[tuple[float, float]],
               window_start_sec: int,
               win_size: int,
               label_mode: str = "soft",
               delay_mode: str = "single_10",
               event_types: list[str] | None = None,  # mode 模式才用
               ) -> np.ndarray:
    """
    返回标签数组。
    label_mode:
      "hard" → (win_size,)，0/1 二值
      "soft" → (win_size,)，每秒填充 ratio
      "mode" → (win_size,)，整窗填充事件类型众数（DRIVEN 风格）
    delay_mode:
      "none"      → delay = 0
      "single_10" → delay = 10
      "multi"     → 返回 (n_delays, win_size)，对每个 delay 生成一份标签
    """
```

### `windowing.py`

```python
def iterate_windows(sleep_onset: float, sleep_offset: float,
                    win_size: int, stride: int):
    """
    生成 t_start 序列。
    """
```

---

## 六、验证脚本 `test_reproduce_v2.py` 思路

```python
"""
验证：用 v2 等价配置跑组件化版本，输出 pickle 与原 MESA_v2/delay10_win60_soft1/ 逐位一致。

v2 等价配置：
    trim_strategy="xml"
    hypopnea_confirm="adjacent"
    normalization="none"
    filter_mode="cheby_hp_20hz"
    label_mode="soft"
    delay_mode="single_10"
    win_size=60, stride=30
"""

import pickle
import hashlib
import numpy as np

def hash_pickle(path):
    with open(path, "rb") as f:
        return hashlib.sha256(f.read()).hexdigest()

def compare_arrays(old, new):
    """逐元素 byte-level 比对。"""
    assert len(old) == len(new), f"长度不匹配 {len(old)} vs {len(new)}"
    for i, (a, b) in enumerate(zip(old, new)):
        if not np.array_equal(a, b):
            diff = np.max(np.abs(a - b))
            raise AssertionError(f"第 {i} 个数组不匹配, max diff = {diff}")

# 跑组件化版本
preprocess_modular.preprocess_main(
    ...,
    experiment_id="row_02_v2_baseline",
)

# 比对
for fold in range(5):
    for split in ["train", "test"]:
        for name in ["x", "y", "t_starts"]:
            old_path = f"MESA_v2/delay10_win60_soft1/mesa_fold{fold}_{name}_{split}.pickle"
            new_path = f"ra_studies/MESA_ablation/row_02_v2_baseline/mesa_fold{fold}_{name}_{split}.pickle"

            # 先做哈希比对
            if hash_pickle(old_path) == hash_pickle(new_path):
                print(f"✅ {name}_{split} fold{fold} 完全一致")
                continue

            # 不一致则做数组级比对
            with open(old_path, "rb") as f: old = pickle.load(f)
            with open(new_path, "rb") as f: new = pickle.load(f)
            compare_arrays(old, new)
            print(f"⚠️ {name}_{split} fold{fold} 哈希不一致但数组逐元素相等")
```

**验证通过的标准**：5 折 × 2 split × 3 pickle = 30 个文件**全部 byte-level 一致**。

如果不一致，说明组件化重构引入了非预期的浮点差异（比如循环顺序、numpy 数据类型）——必须修复后才能进 Week 2。

---

## 七、需要审核确认的事项

请你逐项给出意见：

1. **设计选择 1（C3 归一化时机）**：同意我的建议（选 C）吗？
2. **设计选择 2（C6 多延迟形状）**：同意我的建议（选 A）吗？
3. **设计选择 3（Flow 严格判定阈值）**：同意先用宽松版本（60s 中位数基线 + ±5s 窗口 + 30% 下降）吗？
4. **设计选择 4（输出目录命名）**：同意按行号（A）吗？
5. **文件结构**：`ra_studies/preprocessing_components/` 下 7 个子文件 + 1 个 flow_severity 子模块的拆分粒度合适吗？还是想要更细 / 更粗？
6. **验证标准**：要求 v2 等价配置下产出与原 `MESA_v2/delay10_win60_soft1/` byte-level 一致，这个标准你同意吗？还是允许"数值相等但哈希不同"（如 numpy 版本差异导致字节序不同）？

确认后我立即开始 Step 2 ：实现组件化主入口 + 7 个组件文件。
